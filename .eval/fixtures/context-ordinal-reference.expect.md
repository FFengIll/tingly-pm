# context-ordinal-reference

TURN 1: task "接口重构" created with p0 priority (labeled as "任务A")
TURN 2: task "数据库优化" created with p1 priority (labeled as "任务B")
TURN 3: resolves "第一个任务" as "接口重构" (earliest created), archives as done without asking for ID
TURN 4: lists tasks showing "接口重构" archived, "数据库优化" still active
